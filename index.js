'use strict';

/**
 * Adds commas to a number
 * @param {number} number
 * @param {string} locale
 * @return {string}
 */

function picPipe () {
	console.log('go');
}

exports.mathStuff = function(number, locale) {
    return number.toLocaleString(locale);
};